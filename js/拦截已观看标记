const method = $request.method;

// 如果是 POST（标记已看），直接拦截并返回 200 OK，不发给服务器
if (method === "POST") {
    $done({ response: { status: 200, body: "{}" } });
} else {
    // 如果是 DELETE（取消已看）或其他，直接放行，正常发送给服务器
    $done({});
}
