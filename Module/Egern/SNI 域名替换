name: "SNI 域名替换(Egern版)"
description: "将域名重写到指定目标域名，绕过 SNI 阻断"
icon: "https://raw.githubusercontent.com/xushier/HD-Icons/main/circle/ddns-1.png"
compat_arguments:
  RuleBlock: true
  domain: ""
compat_arguments_schema:
  RuleBlock:
    name: "允许播放进度上报"
    description: "允许播放进度规则"
    default_value: true
    options:
    - true
    - false
  domain:
    name: "域名"
    description: "要替换的域名"
    default_value: ""
rules:
- url_regex:
    name: "拦截数据上报"
    match: "/Sessions/Playing(/Progress|/Stopped)?"
    policy: "REJECT-DROP"
    disabled: {{{RuleBlock}}}
url_rewrites:
- match: ^https?://.*uhd.*/.*(?:System/Ext/ServerDomains|emby/Sessions/Playing)
  location: http://reject/
  status_code: 307
- match: "^https?://(emby|uhd)\\.com"
  location: "https://$1.{{{domain}}}"
  status_code: 307
mitm:
  hostnames:
    includes:
    - "emby.com"
    - "uhd.com"